const app =require('./server')

app.listen(2000, () => {
	console.log('Example app listening at http://localhost:2000')
})


// Observação:
//A API aqui no repl.it está na porta 2000 contudo no PC tem que ser a porta 3000.